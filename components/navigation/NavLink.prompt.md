Header nav item. `default` shows no rule; real mouse hover (or `state="hover"`) and `state="current"` both show the brass underline — the rule doing navigational work, per the brand doc.

```jsx
<NavLink state="current" onDark>Sectors</NavLink>
```
