/* JWL — enquiry form recipients.
   Each Web3Forms access key sends the enquiry to the address it was
   registered with. Add one key per recipient.

   To set up:
     1. Go to web3forms.com, enter shane@jwl.ie, copy the access key.
     2. Do the same for roisin@jwl.ie.
     3. Paste both below, replacing the placeholder text.

   Later, a single group address (e.g. team@jwl.ie) can replace both with
   one key — then Shane manages who receives enquiries from his email
   admin rather than from this file. */
window.JWL_FORM_KEYS = [
  'REPLACE-WITH-SHANE-ACCESS-KEY',   // shane@jwl.ie
  'REPLACE-WITH-ROISIN-ACCESS-KEY'   // roisin@jwl.ie
];
